{
  "name": "freedive-ee",
  "version": "1.0.0",
  "description": "freedive.ee — Freediving in Estonia destination site (Astro, static)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test:smoke": "node scripts/smoke.mjs",
    "test": "npm run build && npm run test:smoke"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.2",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "axe-core": "^4.13.0",
    "playwright": "^1.62.1",
    "typescript": "^6.0.3"
  }
}
